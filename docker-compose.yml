version: '3.9'

services:
  db:
    extends:
      file: .devcontainer/docker-compose.yml
      service: db
    ports:
      - 5432:5432
  test-db:
    extends:
      file: .devcontainer/docker-compose.yml
      service: test-db
  cache:
    extends:
      file: .devcontainer/docker-compose.yml
      service: cache
    ports:
      - 6379:6379

volumes:
  postgres-data:
